import React from 'react';
import { hot } from 'react-hot-loader'

const App = () => <div style={{fontSize: "48px", fontFamily: "Helvetica, sans-serif", fontWeight: "700", display: "flex", flexDirection: "column", alignItems: "center"}}>Get coding!</div>

export default hot(module)(App);